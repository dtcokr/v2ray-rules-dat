name: Build V2Ray rules dat files

on:
  workflow_dispatch:
  schedule:
    - cron: '0 22 * * *'
  push:
    branches:
      - 'hidden'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Go 1.x.y
        uses: actions/setup-go@v2
        with:
          go-version: ^1.18

      - name: Set variables
        run: |
          echo "RELEASE_NAME=Released on $(date +%Y%m%d%H%M)" >> $GITHUB_ENV
          echo "TAG_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
          echo "CHINA_DOMAINS_URL=https://raw.githubusercontent.com/felixonmars/dnsmasq-china-list/master/accelerated-domains.china.conf" >> $GITHUB_ENV
          echo "GOOGLE_DOMAINS_URL=https://raw.githubusercontent.com/felixonmars/dnsmasq-china-list/master/google.china.conf" >> $GITHUB_ENV
          echo "APPLE_DOMAINS_URL=https://raw.githubusercontent.com/felixonmars/dnsmasq-china-list/master/apple.china.conf" >> $GITHUB_ENV
          echo "GFWLIST_DOMAINS_URL=https://cokebar.github.io/gfwlist2dnsmasq/gfwlist_domain.txt" >> $GITHUB_ENV
          echo "GFWLIST_EXTRAS_DOMAINS_URL=https://raw.githubusercontent.com/pexcn/gfwlist-extras/master/gfwlist-extras.txt" >> $GITHUB_ENV
          echo "GREATFIRE_DOMAINS_URL=https://raw.githubusercontent.com/Loyalsoldier/cn-blocked-domain/release/domains.txt" >> $GITHUB_ENV
          echo "EASYLISTCHINA_EASYLIST_REJECT_URL=https://easylist-downloads.adblockplus.org/easylistchina+easylist.txt" >> $GITHUB_ENV
          echo "PETERLOWE_REJECT_URL=https://pgl.yoyo.org/adservers/serverlist.php?hostformat=hosts&showintro=1&mimetype=plaintext" >> $GITHUB_ENV
          echo "ADGUARD_DNS_REJECT_URL=https://adguardteam.github.io/AdGuardSDNSFilter/Filters/filter.txt" >> $GITHUB_ENV
          echo "DANPOLLOCK_REJECT_URL=https://someonewhocares.org/hosts/hosts" >> $GITHUB_ENV
          echo "CUSTOM_DIRECT=https://raw.githubusercontent.com/Loyalsoldier/domain-list-custom/release/cn.txt" >> $GITHUB_ENV
          echo "CUSTOM_PROXY=https://raw.githubusercontent.com/Loyalsoldier/domain-list-custom/release/geolocation-!cn.txt" >> $GITHUB_ENV
          echo "WIN_SPY=https://raw.githubusercontent.com/crazy-max/WindowsSpyBlocker/master/data/hosts/spy.txt" >> $GITHUB_ENV
          echo "WIN_UPDATE=https://raw.githubusercontent.com/crazy-max/WindowsSpyBlocker/master/data/hosts/update.txt" >> $GITHUB_ENV
          echo "WIN_EXTRA=https://raw.githubusercontent.com/crazy-max/WindowsSpyBlocker/master/data/hosts/extra.txt" >> $GITHUB_ENV

          echo "CUSTOM_DEVELOPERDAN_ADS_TRACKING=https://www.github.developerdan.com/hosts/lists/ads-and-tracking-extended.txt" >> $GITHUB_ENV
          echo "CUSTOM_POLISHFILTERSTEAM_KADHOSTS=https://raw.githubusercontent.com/PolishFiltersTeam/KADhosts/master/KADhosts.txt" >> $GITHUB_ENV
          echo "CUSTOM_VELESILA_YHOSTS=https://raw.githubusercontent.com/VeleSila/yhosts/master/hosts" >> $GITHUB_ENV
          echo "CUSTOM_MATOMO_REFERRER_SPAM=https://raw.githubusercontent.com/matomo-org/referrer-spam-blacklist/master/spammers.txt" >> $GITHUB_ENV
          echo "CUSTOM_WINHELP2002=https://winhelp2002.mvps.org/hosts.txt" >> $GITHUB_ENV
          echo "CUSTOM_NEOFELHZ_NEOHOSTS=https://v.firebog.net/hosts/neohostsbasic.txt" >> $GITHUB_ENV
          echo "CUSTOM_ROONEYMCNIBNUG_SNAFU=https://raw.githubusercontent.com/RooneyMcNibNug/pihole-stuff/master/SNAFU.txt" >> $GITHUB_ENV
          echo "CUSTOM_PAULGB_BARBBLOCK=https://paulgb.github.io/BarbBlock/blacklists/hosts-file.txt" >> $GITHUB_ENV
          echo "CUSTOM_ADAWAY=https://adaway.org/hosts.txt" >> $GITHUB_ENV
          echo "CUSTOM_LANIKSJ_ADMIRAL=https://v.firebog.net/hosts/Admiral.txt" >> $GITHUB_ENV
          echo "CUSTOM_ANUDEEPND_BLACKLIST=https://raw.githubusercontent.com/anudeepND/blacklist/master/adservers.txt" >> $GITHUB_ENV
          echo "CUSTOM_DISCONNECT_ADS=https://s3.amazonaws.com/lists.disconnect.me/simple_ad.txt" >> $GITHUB_ENV
          echo "CUSTOM_HOSTSVN=https://raw.githubusercontent.com/bigdargon/hostsVN/master/hosts" >> $GITHUB_ENV
          echo "CUSTOM_JDLINGYU_ADWARS=https://raw.githubusercontent.com/jdlingyu/ad-wars/master/hosts" >> $GITHUB_ENV
          echo "CUSTOM_EASYPRIVACY=https://v.firebog.net/hosts/Easyprivacy.txt" >> $GITHUB_ENV
          echo "CUSTOM_FABRICEPRIGENT_ADS=https://v.firebog.net/hosts/Prigent-Ads.txt" >> $GITHUB_ENV
          echo "CUSTOM_FADEMIND_TRACKERS=https://raw.githubusercontent.com/FadeMind/hosts.extras/master/add.2o7Net/hosts" >> $GITHUB_ENV
          echo "CUSTOM_GEOFFREYFROGEYE_FIRSTPARTY=https://hostfiles.frogeye.fr/firstparty-trackers-hosts.txt" >> $GITHUB_ENV
          echo "CUSTOM_KEES1958_TOP_ADS_TRACKERS=https://raw.githubusercontent.com/Kees1958/W3C_annual_most_used_survey_blocklist/master/TOP_EU_US_Ads_Trackers_HOST" >> $GITHUB_ENV
          echo "CUSTOM_GEOFFREYFROGEYE_MULTIPARTY=https://hostfiles.frogeye.fr/multiparty-trackers-hosts.txt" >> $GITHUB_ENV
          echo "CUSTOM_PERFLYST_ANDROID_TRACKERS=https://raw.githubusercontent.com/Perflyst/PiHoleBlocklist/master/android-tracking.txt" >> $GITHUB_ENV
          echo "CUSTOM_PERFLYST_SMARTTV=https://raw.githubusercontent.com/Perflyst/PiHoleBlocklist/master/SmartTV.txt" >> $GITHUB_ENV
          echo "CUSTOM_QUIDSUP_TRACKERS=https://gitlab.com/quidsup/notrack-blocklists/raw/master/notrack-blocklist.txt" >> $GITHUB_ENV
          echo "CUSTOM_DANDELIONSPROUT_ANTI_MALWARE=https://raw.githubusercontent.com/DandelionSprout/adfilt/master/Alternate%20versions%20Anti-Malware%20List/AntiMalwareHosts.txt" >> $GITHUB_ENV
          echo "CUSTOM_DIGITALSIDE_THREATINTEL=https://osint.digitalside.it/Threat-Intel/lists/latestdomains.txt" >> $GITHUB_ENV
          echo "CUSTOM_FABRICEPRIGENT_CRYPTOJACKING=https://v.firebog.net/hosts/Prigent-Crypto.txt" >> $GITHUB_ENV
          echo "CUSTOM_DISCONNECT_MALVERTISING=https://s3.amazonaws.com/lists.disconnect.me/simple_malvertising.txt" >> $GITHUB_ENV
          echo "CUSTOM_MANDIANT_APT1_REPORT=https://bitbucket.org/ethanr/dns-blacklists/raw/8575c9f96e5b4a1308f2f12394abd86d0927a4a0/bad_lists/Mandiant_APT1_Report_Appendix_D.txt" >> $GITHUB_ENV
          echo "CUSTOM_PHISHINGARMY=https://phishing.army/download/phishing_army_blocklist.txt" >> $GITHUB_ENV
          echo "CUSTOM_QUIDSUP_MALICIOUS=https://gitlab.com/quidsup/notrack-blocklists/raw/master/notrack-malware.txt" >> $GITHUB_ENV
          echo "CUSTOM_SHALLA_MALICIOUS=https://v.firebog.net/hosts/Shalla-mal.txt" >> $GITHUB_ENV
          echo "CUSTOM_SPAM404=https://raw.githubusercontent.com/Spam404/lists/master/main-blacklist.txt" >> $GITHUB_ENV
          echo "CUSTOM_FADEMIND_RISKY_HOSTS=https://raw.githubusercontent.com/FadeMind/hosts.extras/master/add.Risk/hosts" >> $GITHUB_ENV
          echo "CUSTOM_URLHAUS_MALICIOUS=https://urlhaus.abuse.ch/downloads/hostfile/" >> $GITHUB_ENV
          echo "CUSTOM_ZERODOT1_COINBLOCKLISTS=https://zerodot1.gitlab.io/CoinBlockerLists/hosts_browser" >> $GITHUB_ENV
          echo "CUSTOM_ANUDEEPND_FACEBOOK=https://raw.githubusercontent.com/anudeepND/blacklist/master/facebook.txt" >> $GITHUB_ENV
          echo "CUSTOM_PRIVACYPROTECTIONTOOLS_ANTIAD=https://raw.githubusercontent.com/privacy-protection-tools/anti-AD/master/anti-ad-domains.txt" >> $GITHUB_ENV
          echo "CUSTOM_DEVINEENGINE_HIJACKING=https://raw.githubusercontent.com/DivineEngine/Profiles/master/Quantumult/Filter/Guard/Hijacking.list" >> $GITHUB_ENV
          echo "CUSTOM_DEVINEENGINE_ADVERTISING=https://raw.githubusercontent.com/DivineEngine/Profiles/master/Quantumult/Filter/Guard/Advertising.list" >> $GITHUB_ENV
          echo "CUSTOM_DEVINEENGINE_PRIVACY=https://raw.githubusercontent.com/DivineEngine/Profiles/master/Quantumult/Filter/Guard/Privacy.list" >> $GITHUB_ENV
          echo "CUSTOM_EHPO1_LIBY=https://raw.githubusercontent.com/eHpo1/Rules/master/QuantumultX/Filter/Liby.txt" >> $GITHUB_ENV
          echo "CUSTOM_GEQ1AN_ADBLOCK=https://raw.githubusercontent.com/GeQ1an/Rules/master/QuantumultX/Filter/AdBlock.list" >> $GITHUB_ENV
          echo "CUSTOM_NOBYDA_ADBLOCK=https://raw.githubusercontent.com/NobyDa/ND-AD/master/QuantumultX/AD_Block.txt" >> $GITHUB_ENV
          echo "CUSTOM_URLHAUS_MALICIOUS_FULL=https://curben.gitlab.io/malware-filter/urlhaus-filter-hosts.txt" >> $GITHUB_ENV
          echo "CUSTOM_STEVENBLACK_FG=https://raw.githubusercontent.com/StevenBlack/hosts/master/alternates/fakenews-gambling/hosts" >> $GITHUB_ENV
          echo "CUSTOM_NOTRACKING_BLOCKLISTS=https://raw.githubusercontent.com/notracking/hosts-blocklists/master/dnscrypt-proxy/dnscrypt-proxy.blacklist.txt" >> $GITHUB_ENV
          echo "CUSTOM_HOSHSADIQ_NOCOIN=https://raw.githubusercontent.com/hoshsadiq/adblock-nocoin-list/master/hosts.txt" >> $GITHUB_ENV
          
        shell: bash

      - name: Checkout the "hidden" branch of this repo
        uses: actions/checkout@v3
        with:
          ref: hidden
      
      - name: Checkout huh
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.HUH_REPO }}
          token: ${{ secrets.GH_PAT }}
          path: huh

      - name: Checkout Loyalsoldier/domain-list-custom
        uses: actions/checkout@v3
        with:
          repository: Loyalsoldier/domain-list-custom
          path: custom

      - name: Checkout v2fly/domain-list-community
        uses: actions/checkout@v3
        with:
          repository: v2fly/domain-list-community
          path: community

      - name: Checkout cokebar/gfwlist2dnsmasq
        uses: actions/checkout@v3
        with:
          repository: cokebar/gfwlist2dnsmasq
          path: gfwlist2dnsmasq

      - name: Get geoip.dat relative files
        run: |
          wget https://github.com/dtcokr/geoip/raw/release/geoip.dat
          wget https://github.com/dtcokr/geoip/raw/release/geoip.dat.sha256sum

      - name: Generate GFWList domains
        run: |
          cd gfwlist2dnsmasq || exit 1
          chmod +x ./gfwlist2dnsmasq.sh
          ./gfwlist2dnsmasq.sh -l -o ./temp-gfwlist.txt

      - name: Get and add direct domains into temp-direct.txt file
        run: |
          curl -sSL $CHINA_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "$1\n"' > temp-direct.txt
          curl -sSL ${CUSTOM_DIRECT} | perl -ne '/^(domain):([^:]+)(\n$|:@.+)/ && print "$2\n"' >> temp-direct.txt

      - name: Create a BLACKHOLE, for switching off lists temporarily
        run: |
          touch BLACKHOLE
          
      - name: Get and add proxy domains into temp-proxy.txt file
        run: |
          cat ./gfwlist2dnsmasq/temp-gfwlist.txt | perl -ne '/^((?=^.{3,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/ && print "$1\n"' > temp-proxy.txt
          curl -sSL $GFWLIST_EXTRAS_DOMAINS_URL | perl -ne 'print if not /^(#|\s)/' >> temp-proxy.txt
          curl -sSL $GREATFIRE_DOMAINS_URL | perl -ne '/^((?=^.{3,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/ && print "$1\n"' >> temp-proxy.txt
          curl -sSL $GOOGLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "$1\n"' >> temp-proxy.txt
          curl -sSL $APPLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "$1\n"' >> temp-proxy.txt
          curl -sSL ${CUSTOM_PROXY} | grep -Ev ":@cn" | perl -ne '/^(domain):([^:]+)(\n$|:@.+)/ && print "$2\n"' >> temp-proxy.txt

      - name: Get and add reject domains into temp-reject.txt file
        run: |
          echo "# EASYLISTCHINA_EASYLIST_REJECT_URL" > temp-reject.txt
          curl -sSL $EASYLISTCHINA_EASYLIST_REJECT_URL | perl -ne '/^\|\|([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\^$/ && print "$1\n"' | perl -ne 'print if not /^[0-9]{1,3}(\.[0-9]{1,3}){3}$/' >> temp-reject.txt
          
          echo "# ADGUARD_DNS_REJECT_URL" >> temp-reject.txt
          curl -sSL $ADGUARD_DNS_REJECT_URL | perl -ne '/^\|\|([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\^$/ && print "$1\n"' | perl -ne 'print if not /^[0-9]{1,3}(\.[0-9]{1,3}){3}$/' >> temp-reject.txt
          
          echo "# PETERLOWE_REJECT_URL" >> temp-reject.txt
          curl -sSL $PETERLOWE_REJECT_URL | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> temp-reject.txt
          
          echo "# DANPOLLOCK_REJECT_URL" >> temp-reject.txt
          curl -sSL $DANPOLLOCK_REJECT_URL | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' | sed '1d' >> temp-reject.txt

      - name: Get and add CUSTOM domains into temp-reject.txt file
        run: |
          echo "# CUSTOM_DEVELOPERDAN_ADS_TRACKING" >> BLACKHOLE
          curl -sSL ${CUSTOM_DEVELOPERDAN_ADS_TRACKING} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_POLISHFILTERSTEAM_KADHOSTS" >> BLACKHOLE
          curl -sSL ${CUSTOM_POLISHFILTERSTEAM_KADHOSTS} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_VELESILA_YHOSTS" >> BLACKHOLE
          curl -sSL ${CUSTOM_VELESILA_YHOSTS} | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_MATOMO_REFERRER_SPAM" >> BLACKHOLE
          curl -sSL ${CUSTOM_MATOMO_REFERRER_SPAM} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_WINHELP2002" >> BLACKHOLE
          curl -sSL ${CUSTOM_WINHELP2002} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_NEOFELHZ_NEOHOSTS" >> BLACKHOLE
          curl -sSL ${CUSTOM_NEOFELHZ_NEOHOSTS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_ROONEYMCNIBNUG_SNAFU" >> BLACKHOLE
          curl -sSL ${CUSTOM_ROONEYMCNIBNUG_SNAFU} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_PAULGB_BARBBLOCK" >> BLACKHOLE
          curl -sSL ${CUSTOM_PAULGB_BARBBLOCK} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_ADAWAY" >> temp-reject.txt
          curl -sSL ${CUSTOM_ADAWAY} | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_LANIKSJ_ADMIRAL" >> BLACKHOLE
          curl -sSL ${CUSTOM_LANIKSJ_ADMIRAL} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_ANUDEEPND_BLACKLIST" >> BLACKHOLE
          curl -sSL ${CUSTOM_ANUDEEPND_BLACKLIST} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_DISCONNECT_ADS" >> BLACKHOLE
          curl -sSL ${CUSTOM_DISCONNECT_ADS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_HOSTSVN" >> BLACKHOLE
          curl -sSL ${CUSTOM_HOSTSVN} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_JDLINGYU_ADWARS" >> temp-reject.txt
          curl -sSL ${CUSTOM_JDLINGYU_ADWARS} | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_EASYPRIVACY" >> BLACKHOLE
          curl -sSL ${CUSTOM_EASYPRIVACY} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_FABRICEPRIGENT_ADS" >> BLACKHOLE
          curl -sSL ${CUSTOM_FABRICEPRIGENT_ADS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_FADEMIND_TRACKERS" >> BLACKHOLE
          curl -sSL ${CUSTOM_FADEMIND_TRACKERS} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_GEOFFREYFROGEYE_FIRSTPARTY" >> BLACKHOLE
          curl -sSL ${CUSTOM_GEOFFREYFROGEYE_FIRSTPARTY} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_KEES1958_TOP_ADS_TRACKERS" >> BLACKHOLE
          curl -sSL ${CUSTOM_KEES1958_TOP_ADS_TRACKERS} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_GEOFFREYFROGEYE_MULTIPARTY" >> BLACKHOLE
          curl -sSL ${CUSTOM_GEOFFREYFROGEYE_MULTIPARTY} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_PERFLYST_ANDROID_TRACKERS" >> BLACKHOLE
          curl -sSL ${CUSTOM_PERFLYST_ANDROID_TRACKERS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_PERFLYST_SMARTTV" >> temp-reject.txt
          curl -sSL ${CUSTOM_PERFLYST_SMARTTV} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_QUIDSUP_TRACKERS" >> BLACKHOLE
          curl -sSL ${CUSTOM_QUIDSUP_TRACKERS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_DANDELIONSPROUT_ANTI_MALWARE" >> BLACKHOLE
          curl -sSL ${CUSTOM_DANDELIONSPROUT_ANTI_MALWARE} | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_DIGITALSIDE_THREATINTEL" >> BLACKHOLE
          curl -sSL ${CUSTOM_DIGITALSIDE_THREATINTEL} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_FABRICEPRIGENT_CRYPTOJACKING" >> BLACKHOLE
          curl -sSL ${CUSTOM_FABRICEPRIGENT_CRYPTOJACKING} | perl -ne '/^0\.0\.0\.0([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_DISCONNECT_MALVERTISING" >> BLACKHOLE
          curl -sSL ${CUSTOM_DISCONNECT_MALVERTISING} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_MANDIANT_APT1_REPORT" >> BLACKHOLE
          curl -sSL ${CUSTOM_MANDIANT_APT1_REPORT} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_PHISHINGARMY" >> temp-reject.txt
          curl -sSL ${CUSTOM_PHISHINGARMY} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_QUIDSUP_MALICIOUS" >> BLACKHOLE
          curl -sSL ${CUSTOM_QUIDSUP_MALICIOUS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_SHALLA_MALICIOUS" >> BLACKHOLE
          curl -sSL ${CUSTOM_SHALLA_MALICIOUS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_SPAM404" >> temp-reject.txt
          curl -sSL ${CUSTOM_SPAM404} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_FADEMIND_RISKY_HOSTS" >> BLACKHOLE
          curl -sSL ${CUSTOM_FADEMIND_RISKY_HOSTS} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_URLHAUS_MALICIOUS" >> BLACKHOLE
          curl -sSL ${CUSTOM_URLHAUS_MALICIOUS} | perl -ne '/^127\.0\.0\.1\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_ZERODOT1_COINBLOCKLISTS" >> BLACKHOLE
          curl -sSL ${CUSTOM_ZERODOT1_COINBLOCKLISTS} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_ANUDEEPND_FACEBOOK" >> BLACKHOLE
          curl -sSL ${CUSTOM_ANUDEEPND_FACEBOOK} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_PRIVACYPROTECTIONTOOLS_ANTIAD" >> BLACKHOLE
          curl -sSL ${CUSTOM_PRIVACYPROTECTIONTOOLS_ANTIAD} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_DEVINEENGINE_HIJACKING" >> temp-reject.txt
          curl -sSL ${CUSTOM_DEVINEENGINE_HIJACKING} | perl -ne '/^DOMAIN-SUFFIX\,([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\,REJECT$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_DEVINEENGINE_ADVERTISING" >> temp-reject.txt
          curl -sSL ${CUSTOM_DEVINEENGINE_ADVERTISING} | perl -ne '/^DOMAIN-SUFFIX\,([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\,REJECT$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_DEVINEENGINE_PRIVACY" >> temp-reject.txt
          curl -sSL ${CUSTOM_DEVINEENGINE_PRIVACY} | perl -ne '/^DOMAIN-SUFFIX\,([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\,REJECT$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_EHPO1_LIBY" >> BLACKHOLE
          curl -sSL ${CUSTOM_EHPO1_LIBY} | perl -ne '/^(host-suffix|host)\,\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\,\sAdBlock$/ && print "$2\n"' >> BLACKHOLE

          echo "# CUSTOM_GEQ1AN_ADBLOCK" >> temp-reject.txt
          curl -sSL ${CUSTOM_GEQ1AN_ADBLOCK} | perl -ne '/^HOST-SUFFIX\,([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\,AdBlock$/ && print "$1\n"' >> temp-reject.txt

          echo "# CUSTOM_NOBYDA_ADBLOCK" >> temp-reject.txt
          curl -sSL ${CUSTOM_NOBYDA_ADBLOCK} | perl -ne '/^DOMAIN-SUFFIX\,([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})\,REJECT$/ && print "$1\n"' >> temp-reject.txt
          
          echo "# CUSTOM_URLHAUS_MALICIOUS_FULL" >> BLACKHOLE
          curl -sSL ${CUSTOM_URLHAUS_MALICIOUS_FULL} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_STEVENBLACK_FG" >> BLACKHOLE
          curl -sSL ${CUSTOM_STEVENBLACK_FG} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_NOTRACKING_BLOCKLISTS" >> BLACKHOLE
          curl -sSL ${CUSTOM_NOTRACKING_BLOCKLISTS} | perl -ne '/^([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

          echo "# CUSTOM_HOSHSADIQ_NOCOIN">> BLACKHOLE
          curl -sSL ${CUSTOM_HOSHSADIQ_NOCOIN} | perl -ne '/^0\.0\.0\.0\s([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})$/ && print "$1\n"' >> BLACKHOLE

      - name: Reserve `full`, `regexp` and `keyword` type of rules from custom lists to "reserve" files
        run: |
          curl -sSL ${CUSTOM_DIRECT} | perl -ne '/^((full|keyword):[^:]+)(\n$|:@.+)/ && print "$1\n"' | sort --ignore-case -u > direct-reserve.txt
          curl -sSL ${CUSTOM_PROXY} | grep -Ev ":@cn" | perl -ne '/^((full|keyword):[^:]+)(\n$|:@.+)/ && print "$1\n"' | sort --ignore-case -u > proxy-reserve.txt

      - name: Add proxy, direct and reject domains from "hidden" branch to appropriate temp files
        run: |
          sed -i '/^$/d' proxy.txt
          sed -i '/^$/d' direct.txt
          sed -i '/^$/d' reject.txt
          sed -i '/^$/d' proxy-ip.txt
          sed -i '/^$/d' direct-ip.txt
          sed -i '/^$/d' reject-ip.txt
          cat proxy.txt | perl -ne '/^(?!#)([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> temp-proxy.txt
          cat ./huh/proxy-always.txt | perl -ne '/^(?!#)([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> temp-proxy.txt
          cat direct.txt | perl -ne '/^(?!#)([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> temp-direct.txt
          cat ./huh/direct-always.txt | perl -ne '/^(?!#)([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> temp-direct.txt
          echo "# CUSTOM_FROM_HIDDEN" >> temp-reject.txt
          cat reject.txt | perl -ne '/^(?!#)([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> temp-reject.txt
          cat ./huh/reject-always.txt | perl -ne '/^(?!#)([-_0-9a-zA-Z]+(\.[-_0-9a-zA-Z]+){1,64})/ && print "$1\n"' >> temp-reject.txt
          
      - name: Sort and generate non-redundant lists
        run: |
          cat temp-proxy.txt | sort --ignore-case -u > proxy-list-without-redundant
          cat temp-direct.txt | sort --ignore-case -u > direct-list-without-redundant
          cat temp-reject.txt | awk '{gsub(/^www\./,"",$1);print}' | sort --ignore-case -u > reject-list-without-redundant

      - name: Taging reject domains with source
        run: |
          chmod +x ./adlist_tagging.py
          ./adlist_tagging.py temp-reject.txt reject-list-with-tag
          sort --ignore-case -u ./reject-list-with-tag > reject-list-with-tag-sorted
          cat ./reject-list-with-tag-sorted > reject-list-with-tag.txt

      - name: Prepare for "reject-need-to-remove"
        run: |
          chmod +x ./domain_generalization.py
          ./domain_generalization.py ./huh/reject-need-to-remove.txt reject-list-without-redundant reject-need-to-remove-generalization
          cat reject-need-to-remove-generalization >> reject-need-to-remove.txt

      - name: Remove domains from "need-to-remove" lists in "hidden" branch and sort
        run: |
          sort --ignore-case -u ./huh/direct-need-to-remove.txt > ./direct-need-to-remove-sorted.txt
          sort --ignore-case -u ./huh/proxy-need-to-remove.txt > ./proxy-need-to-remove-sorted.txt
          sort --ignore-case -u ./reject-need-to-remove.txt > ./reject-need-to-remove-sorted.txt
          
          sort --ignore-case -u ./direct.txt > ./direct-sorted.txt
          sort --ignore-case -u ./proxy.txt > ./proxy-sorted.txt
          sort --ignore-case -u ./reject.txt > ./reject-sorted.txt

          chmod +x findRedundantDomain.py
          ./findRedundantDomain.py ./direct-sorted.txt ./direct-redundant
          ./findRedundantDomain.py ./proxy-sorted.txt ./proxy-redundant
          ./findRedundantDomain.py ./reject-sorted.txt ./reject-redundant

          [ ! -f "direct-redundant" ] && touch direct-redundant
          [ ! -f "proxy-redundant" ] && touch proxy-redundant
          [ ! -f "reject-redundant" ] && touch reject-redundant

          sort --ignore-case -u direct-redundant > direct-redundant-sorted
          sort --ignore-case -u proxy-redundant > proxy-redundant-sorted
          sort --ignore-case -u reject-redundant > reject-redundant-sorted

          diff ./direct-redundant-sorted ./direct-sorted.txt | awk '/^>/{print $2}' > ./direct-clean
          diff ./proxy-redundant-sorted ./proxy-sorted.txt | awk '/^>/{print $2}' > ./proxy-clean
          diff ./reject-redundant-sorted ./reject-sorted.txt | awk '/^>/{print $2}' > ./reject-clean
          
          cat ./direct-need-to-remove-sorted.txt > ./direct-need-to-remove.txt
          cat ./proxy-need-to-remove-sorted.txt > ./proxy-need-to-remove.txt
          cat ./reject-need-to-remove-sorted.txt > ./reject-need-to-remove.txt
          
          cat ./direct-clean > ./direct.txt
          cat ./proxy-clean > ./proxy.txt
          cat ./reject-clean > ./reject.txt
          
          diff ./direct-need-to-remove-sorted.txt ./direct-list-without-redundant | awk '/^>/{print $2}' > temp-cn.txt
          diff ./proxy-need-to-remove-sorted.txt ./proxy-list-without-redundant | awk '/^>/{print $2}' > temp-geolocation-\!cn.txt
          diff ./reject-need-to-remove-sorted.txt ./reject-list-without-redundant | awk '/^>/{print $2}' > temp-category-ads-all.txt

      - name: Remove domains end with ".cn" in "temp-geolocation-!cn.txt" and write lists to data directory
        run: |
          cat temp-cn.txt | sort --ignore-case -u | perl -ne '/^((?=^.{1,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})*)/ && print "$1\n"' > ./community/data/cn
          cat temp-cn.txt | sort --ignore-case -u | perl -ne 'print if not /^((?=^.{3,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/' > direct-tld-list.txt
          cat temp-geolocation-\!cn.txt | sort --ignore-case -u | perl -ne '/^((?=^.{1,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})*)/ && print "$1\n"' | perl -ne 'print if not /\.cn$/' > ./community/data/geolocation-\!cn
          cat temp-geolocation-\!cn.txt | sort --ignore-case -u | perl -ne 'print if not /^((?=^.{3,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/' > proxy-tld-list.txt
          cat temp-category-ads-all.txt | sort --ignore-case -u | perl -ne '/^((?=^.{1,255})[-_a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[-_a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/ && print "$1\n"' > ./community/data/category-ads-all
          cat temp-category-ads-all.txt | sort --ignore-case -u | perl -ne 'print if not /^((?=^.{3,255})[-_a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[-_a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/' > reject-tld-list.txt

      - name: Add `full`, `regexp` and `keyword` type of rules back into "cn", "geolocation-!cn" and "category-ads-all" list
        run: |
          [ -f "direct-reserve.txt" ] && cat direct-reserve.txt >> ./community/data/cn
          [ -f "proxy-reserve.txt" ] && cat proxy-reserve.txt >> ./community/data/geolocation-\!cn
          [ -f "reject-reserve.txt" ] && cat reject-reserve.txt >> ./community/data/category-ads-all
          cp ./community/data/cn direct-list.txt
          cp ./community/data/geolocation-\!cn proxy-list.txt
          cp ./community/data/category-ads-all reject-list.txt

      - name: Create `google-cn`、`apple-cn`、`gfw`、`greatfire`、`apple-cn-ov`、`google-cn-ov` lists
        run: |
          curl -sSL $GOOGLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "full:$1\n"' > ./community/data/google-cn
          curl -sSL $GOOGLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "$1\n"' > google-cn-ov
          curl -sSL $GOOGLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "full:$1\n"' > google-cn.txt

          curl -sSL $APPLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "full:$1\n"' > ./community/data/apple-cn
          curl -sSL $APPLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "$1\n"' > apple-cn-ov
          curl -sSL $APPLE_DOMAINS_URL | perl -ne '/^server=\/([^\/]+)\// && print "full:$1\n"' > apple-cn.txt
          cat ./gfwlist2dnsmasq/temp-gfwlist.txt | perl -ne '/^((?=^.{3,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/ && print "$1\n"' >> ./community/data/gfw
          curl -sSL $GFWLIST_EXTRAS_DOMAINS_URL | perl -ne 'print if not /^(#|\s)/' >> ./community/data/gfw
          cat ./community/data/gfw | sort --ignore-case -u > gfw.txt
          curl -sSL $GREATFIRE_DOMAINS_URL | perl -ne '/^((?=^.{3,255})[a-zA-Z0-9][-_a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-_a-zA-Z0-9]{0,62})+)/ && print "$1\n"' >> ./community/data/greatfire
          cat ./community/data/greatfire | sort --ignore-case -u > greatfire.txt
          curl -sSL $WIN_SPY | grep "0.0.0.0" | awk '{print $2}' > ./community/data/win-spy
          curl -sSL $WIN_SPY | grep "0.0.0.0" | awk '{print $2}' > win-spy.txt
          curl -sSL $WIN_UPDATE | grep "0.0.0.0" | awk '{print $2}' > ./community/data/win-update
          curl -sSL $WIN_UPDATE | grep "0.0.0.0" | awk '{print $2}' > win-update.txt
          curl -sSL $WIN_EXTRA | grep "0.0.0.0" | awk '{print $2}' > ./community/data/win-extra
          curl -sSL $WIN_EXTRA | grep "0.0.0.0" | awk '{print $2}' > win-extra.txt
          
      - name: Remove domains from "need-to-remove" lists in "hidden" branch for `win-spy`, `win-update` and `win-extra`
        run: |
          diff ./reject-need-to-remove-sorted.txt ./community/data/win-spy  | awk '/^>/{print $2}' > temp-win-spy
          diff ./reject-need-to-remove-sorted.txt ./community/data/win-update  | awk '/^>/{print $2}' > temp-win-update
          diff ./reject-need-to-remove-sorted.txt ./community/data/win-extra  | awk '/^>/{print $2}' > temp-win-extra
          
          cat ./temp-win-spy > ./community/data/win-spy
          cat ./temp-win-spy > win-spy.txt
          cat ./temp-win-update > ./community/data/win-update
          cat ./temp-win-update > win-update.txt
          cat ./temp-win-extra > ./community/data/win-extra
          cat ./temp-win-extra > win-extra.txt
          
      - name: Remove redundant domains and create `OverkillAdBlock`, `OverkillAdBlock-CN`, `OverkillAdBlock-Lite`, `OverkillDirect`, `OverkillProxy`, `OverkillAppleCN-DIRECT`, `OverkillGoogleCN-DIRECT` lists
        run: |
          cat direct-list.txt | perl -ne '/(^full\:)([a-zA-Z0-9-_\.]*)/ && print "$2\n"' > ./direct-list-full-domain
          cat proxy-list.txt | perl -ne '/(^full\:)([a-zA-Z0-9-_\.]*)/ && print "$2\n"' > ./proxy-list-full-domain

          sed '/^full\:*/d' ./direct-list.txt > ./direct-list-ov.txt
          cat ./direct-list-full-domain >> ./direct-list-ov.txt

          sed '/^full\:*/d' ./proxy-list.txt > ./proxy-list-ov.txt
          cat ./proxy-list-full-domain >> ./proxy-list-ov.txt

          sort --ignore-case -u ./direct-list-ov.txt > ./direct-list-ov-sorted.txt
          sort --ignore-case -u ./proxy-list-ov.txt > ./proxy-list-ov-sorted.txt
          sort --ignore-case -u ./reject-list.txt > ./reject-list-sorted
          sort --ignore-case -u ./apple-cn-ov > ./apple-cn-ov-sorted
          sort --ignore-case -u ./google-cn-ov > ./google-cn-ov-sorted
          
          cat ./proxy-list-ov-sorted.txt > ./proxy-list-pre-ov
          diff ./reject-list-sorted ./direct-list-ov-sorted.txt | awk '/^>/{print $2}' > ./direct-list-pre-ov
          diff ./reject-list-sorted ./apple-cn-ov-sorted | awk '/^>/{print $2}' > ./apple-cn-pre-ov
          diff ./reject-list-sorted ./google-cn-ov-sorted | awk '/^>/{print $2}' > ./google-cn-pre-ov

          chmod +x findRedundantDomain.py
          ./findRedundantDomain.py ./proxy-list-pre-ov ./proxy-list-pre-ov-redundant
          ./findRedundantDomain.py ./direct-list-pre-ov ./direct-list-pre-ov-redundant
          ./findRedundantDomain.py ./reject-list-sorted ./reject-list-sorted-redundant

          [ ! -f "proxy-list-pre-ov-redundant" ] && touch proxy-list-pre-ov-redundant
          [ ! -f "direct-list-pre-ov-redundant" ] && touch direct-list-pre-ov-redundant
          [ ! -f "reject-list-sorted-redundant" ] && touch reject-list-sorted-redundant

          sort --ignore-case -u proxy-list-pre-ov-redundant > proxy-list-pre-ov-redundant-sorted
          sort --ignore-case -u direct-list-pre-ov-redundant > direct-list-pre-ov-redundant-sorted
          sort --ignore-case -u reject-list-sorted-redundant > reject-list-sorted-redundant-sorted

          diff ./proxy-list-pre-ov-redundant-sorted ./proxy-list-pre-ov | awk '/^>/{print $2}' > ./proxy-list-pre-ov-clean
          cat ./proxy-list-pre-ov-clean > proxy-list-pre-ov
          diff ./direct-list-pre-ov-redundant-sorted ./direct-list-pre-ov | awk '/^>/{print $2}' > ./direct-list-pre-ov-clean
          cat ./direct-list-pre-ov-clean > direct-list-pre-ov
          diff ./reject-list-sorted-redundant-sorted ./reject-list-sorted | awk '/^>/{print $2}' > ./reject-list-sorted-clean
          cat ./reject-list-sorted-clean > reject-list-sorted

          chmod +x find_upper.py findRedundantDomain.py
          ./find_upper.py ./direct.txt ./direct-list-pre-ov direct-upper
          sort -fu direct-upper > direct-upper-sorted
          cat direct.txt >> direct-upper-sorted
          sed -i '/^$/d' ./huh/direct-always.txt
          cat ./huh/direct-always.txt >> direct-upper-sorted
          sort -fu direct-upper-sorted > direct-upper-sorted-2
          ./findRedundantDomain.py direct-upper-sorted-2 direct-upper-sorted-2-redundant
          [ ! -f "direct-upper-sorted-2-redundant" ] && touch direct-upper-sorted-2-redundant
          sort -fu direct-upper-sorted-2-redundant > direct-upper-sorted-2-redundant-sorted
          diff direct-upper-sorted-2-redundant-sorted direct-upper-sorted-2 | awk '/^>/{print $2}' > direct-pre-lean

          chmod +x find_upper.py findRedundantDomain.py
          ./find_upper.py ./proxy.txt ./proxy-list-pre-ov proxy-upper
          sort -fu proxy-upper > proxy-upper-sorted
          cat proxy.txt >> proxy-upper-sorted
          sed -i '/^$/d' ./huh/proxy-always.txt
          cat ./huh/proxy-always.txt >> proxy-upper-sorted
          sort -fu proxy-upper-sorted > proxy-upper-sorted-2
          ./findRedundantDomain.py proxy-upper-sorted-2 proxy-upper-sorted-2-redundant
          [ ! -f "proxy-upper-sorted-2-redundant" ] && touch proxy-upper-sorted-2-redundant
          sort -fu proxy-upper-sorted-2-redundant > proxy-upper-sorted-2-redundant-sorted
          diff proxy-upper-sorted-2-redundant-sorted proxy-upper-sorted-2 | awk '/^>/{print $2}' > proxy-pre-lean

          chmod +x find_upper.py findRedundantDomain.py
          ./find_upper.py ./reject.txt ./reject-list-sorted reject-upper
          sort -fu reject-upper > reject-upper-sorted
          chmod +x cn_reject.py
          ./cn_reject.py reject-list-sorted direct-pre-lean ovalean-d
          cat ovalean-d >> reject-upper-sorted
          cat reject.txt >> reject-upper-sorted
          sed -i '/^$/d' ./huh/reject-always.txt
          cat ./huh/reject-always.txt >> reject-upper-sorted
          sort -fu reject-upper-sorted > reject-upper-sorted-2
          ./findRedundantDomain.py reject-upper-sorted-2 reject-upper-sorted-2-redundant
          [ ! -f "reject-upper-sorted-2-redundant" ] && touch reject-upper-sorted-2-redundant
          sort -fu reject-upper-sorted-2-redundant > reject-upper-sorted-2-redundant-sorted
          diff reject-upper-sorted-2-redundant-sorted reject-upper-sorted-2 | awk '/^>/{print $2}' > reject-pre-lean
          diff ./reject-need-to-remove-sorted.txt ./reject-pre-lean  | awk '/^>/{print $2}' > reject-pre-lean-2
          cat reject-pre-lean-2 > reject-pre-lean

          cat ./reject-list-sorted | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", REJECT",$2);print}' > OverkillAdBlock.list
          cat ./reject-list-sorted | perl -ne '/\.cn$/ && print' | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", REJECT",$2);print}' > OverkillAdBlock-CN.list
          cat ./direct-list-pre-ov | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", DIRECT",$2);print}' > OverkillDirect.list
          cat ./proxy-list-pre-ov | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", PROXY",$2);print}' > OverkillProxy.list
          cat ./apple-cn-pre-ov | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", DIRECT",$2);print}' > OverkillAppleCN-DIRECT.list
          cat ./apple-cn-pre-ov | awk '{gsub(/^/,"DOMAIN-SUFFIX, ",$1);print}' > OverkillAppleCN-DIRECT-RuleSet.list
          cat ./google-cn-pre-ov | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", DIRECT",$2);print}' > OverkillGoogleCN-DIRECT.list
          
          diff ./proxy-pre-lean ./direct-pre-lean | awk '/^>/{print $2}' > ./remove-proxy-from-direct
          sort -fu ./remove-proxy-from-direct > ./remove-proxy-from-direct-sorted
          cat ./remove-proxy-from-direct-sorted > ./remove-proxy-from-direct

          diff ./direct-need-to-remove.txt ./remove-proxy-from-direct | awk '/^>/{print $2}' > ./remove-need-from-direct
          sort -fu ./remove-need-from-direct > ./remove-need-from-direct-sorted
          cat ./remove-need-from-direct-sorted > ./direct-pre-lean
          
          cat ./direct-pre-lean | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", DIRECT",$2);print}' > OverkillDirect-Lean.list
          cat ./direct-pre-lean | awk '{gsub(/^/,"DOMAIN-SUFFIX, ",$1);print}' > OverkillDirect-Lean-RuleSet.list
          cat ./proxy-pre-lean | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", PROXY",$2);print}' > OverkillProxy-Lean.list
          cat ./proxy-pre-lean | awk '{gsub(/^/,"DOMAIN-SUFFIX, ",$1);print}' > OverkillProxy-Lean-RuleSet.list
          diff ./huh/cdn-domain.txt ./reject-list-sorted | awk '/^>/{print $2}' | awk '{gsub(/^/,"||",$1);print}' | awk '{gsub(/$/,"^",$1);print}' > OverkillGuardian.list

          cat proxy-pre-lean > proxy.result
          cat direct-pre-lean > direct.result

          sort -f -u ./proxy-ip.txt > proxy-ip-sorted.txt
          sort -f -u ./direct-ip.txt > direct-ip-sorted.txt
          sort -f -u ./reject-ip.txt > reject-ip-sorted.txt

          cat ./proxy-ip-sorted.txt > proxy-ip.txt
          cat ./direct-ip-sorted.txt > direct-ip.txt
          cat ./reject-ip-sorted.txt > reject-ip.txt

          cat ./proxy-ip.txt >> OverkillProxy.list
          diff proxy-ip.txt ./huh/proxy-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' >> OverkillProxy.list
          
          cat ./proxy-ip.txt >> OverkillProxy-Lean.list
          cat ./proxy-ip.txt | awk '{gsub(/proxy/,"no-resolve",$3);print}' >> OverkillProxy-Lean-RuleSet.list
          diff proxy-ip.txt ./huh/proxy-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' >> OverkillProxy-Lean.list
          diff proxy-ip.txt ./huh/proxy-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' | awk '{gsub(/proxy/,"no-resolve",$3);print}' >> OverkillProxy-Lean-RuleSet.list
          
          cat ./direct-ip.txt >> OverkillDirect.list
          diff direct-ip.txt ./huh/direct-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' >> OverkillDirect.list
          
          cat ./direct-ip.txt >> OverkillDirect-Lean.list
          cat ./direct-ip.txt | awk '{gsub(/direct/,"no-resolve",$3);print}' >> OverkillDirect-Lean-RuleSet.list
          diff direct-ip.txt ./huh/direct-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' >> OverkillDirect-Lean.list
          diff direct-ip.txt ./huh/direct-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' | awk '{gsub(/direct/,"no-resolve",$3);print}' >> OverkillDirect-Lean-RuleSet.list


          chmod +x cn_reject.py
          ./cn_reject.py reject-list-sorted direct-list-ov-sorted.txt ovlite-d
          cat ./reject.txt >> ovalean-d
          sort -fu ovalean-d > ovalean-d-sorted
          cat ovalean-d-sorted > ovalean-d
          cat ./reject-list-sorted | perl -ne '/\.cn$/ && print' > ovlite-c
          cat ovlite-d > pre-ovlite
          cat ovalean-d > pre-ovalean
          cat ovlite-c >> pre-ovlite
          sort --ignore-case -u pre-ovlite > pre-ovlite-sorted
          sort --ignore-case -u pre-ovalean > pre-ovalean-sorted
          cat pre-ovlite-sorted | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", REJECT",$2);print}' > OverkillAdBlock-Lite.list
          cat reject-pre-lean | awk '{gsub(/^/,"HOST-SUFFIX, ",$1);print}' | awk '{gsub(/$/,", REJECT",$2);print}' > OverkillAdBlock-Lean.list
          cat reject-pre-lean | awk '{gsub(/^/,"DOMAIN-SUFFIX, ",$1);print}' > OverkillAdBlock-Lean-RuleSet.list

          cat reject-pre-lean > reject.result

          cat ./reject-ip.txt >> OverkillAdBlock-Lite.list
          diff reject-ip.txt ./huh/reject-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' >> OverkillAdBlock-Lite.list
          cat ./reject-ip.txt >> OverkillAdBlock-Lean.list
          cat ./reject-ip.txt | awk '{gsub(/reject/,"no-resolve",$3);print}' >> OverkillAdBlock-Lean-RuleSet.list
          diff reject-ip.txt ./huh/reject-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' >> OverkillAdBlock-Lean.list
          diff reject-ip.txt ./huh/reject-ip-always.txt | awk '/^>/{print $2" "$3" "$4}' | awk '{gsub(/reject/,"no-resolve",$3);print}' >> OverkillAdBlock-Lean-RuleSet.list

          echo "OVA=$(wc -l < OverkillAdBlock.list)" >> $GITHUB_ENV
          echo "OVACN=$(wc -l < OverkillAdBlock-CN.list)" >> $GITHUB_ENV
          echo "OVAL=$(wc -l < OverkillAdBlock-Lite.list)" >> $GITHUB_ENV
          echo "OVALE=$(wc -l < OverkillAdBlock-Lean.list)" >> $GITHUB_ENV
          echo "OVD=$(wc -l < OverkillDirect.list)" >> $GITHUB_ENV
          echo "OVDLE=$(wc -l < OverkillDirect-Lean.list)" >> $GITHUB_ENV
          echo "OVP=$(wc -l < OverkillProxy.list)" >> $GITHUB_ENV
          echo "OVPLE=$(wc -l < OverkillProxy-Lean.list)" >> $GITHUB_ENV

      - name: Create change log
        run: |
          wget -O direct-list-pre.txt https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/direct-list.txt
          wget -O proxy-list-pre.txt https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/proxy-list.txt
          wget -O reject-list-with-tag-old.txt.xz https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/reject-list-with-tag.txt.xz
          
          xz -d reject-list-with-tag-old.txt.xz
          
          cat direct-list-pre.txt | perl -ne '/(^full\:)([a-zA-Z0-9-_\.]*)/ && print "$2\n"' > ./direct-list-pre-full-domain
          sed '/^full\:*/d' ./direct-list-pre.txt > ./direct-list-pre-all
          cat ./direct-list-pre-full-domain >> ./direct-list-pre-all
          sort --ignore-case -u direct-list-pre-all > direct-list-pre-all-sorted.txt
          
          cat proxy-list-pre.txt | perl -ne '/(^full\:)([a-zA-Z0-9-_\.]*)/ && print "$2\n"' > ./proxy-list-pre-full-domain
          sed '/^full\:*/d' ./proxy-list-pre.txt > ./proxy-list-pre-all
          cat ./proxy-list-pre-full-domain >> ./proxy-list-pre-all
          sort --ignore-case -u proxy-list-pre-all > proxy-list-pre-all-sorted.txt
                    
          sort --ignore-case -u direct-list-ov.txt > direct-list-ov-sorted-2
          sort --ignore-case -u proxy-list-ov.txt > proxy-list-ov-sorted-2
          
          echo '## Changes in `direct-list.txt`' > CHANGELOG.md
          echo "### Add:" >> CHANGELOG.md
          diff ./direct-list-pre-all-sorted.txt ./direct-list-ov-sorted-2 | awk '/^>/{print}' >> CHANGELOG.md
          echo "### Remove:" >> CHANGELOG.md
          diff ./direct-list-pre-all-sorted.txt ./direct-list-ov-sorted-2 | awk '/^</{print}' >> CHANGELOG.md
          echo -e "\n" >> CHANGELOG.md
          
          echo '## Changes in `proxy-list.txt`' >> CHANGELOG.md
          echo "### Add:" >> CHANGELOG.md
          diff ./proxy-list-pre-all-sorted.txt ./proxy-list-ov-sorted-2 | awk '/^>/{print}' >> CHANGELOG.md
          echo "### Remove:" >> CHANGELOG.md
          diff ./proxy-list-pre-all-sorted.txt ./proxy-list-ov-sorted-2 | awk '/^</{print}' >> CHANGELOG.md
          echo -e "\n" >> CHANGELOG.md
          
          echo '## Changes in `reject-list.txt`' >> CHANGELOG.md
          echo "### Add:" >> CHANGELOG.md
          diff ./reject-list-with-tag-old.txt ./reject-list-with-tag.txt | awk '/^>/{print}' >> CHANGELOG.md
          echo "### Remove:" >> CHANGELOG.md
          diff ./reject-list-with-tag-old.txt ./reject-list-with-tag.txt | awk '/^</{print}' >> CHANGELOG.md

          cp CHANGELOG.md CHANGELOG.txt

          wget -O ovacn-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillAdBlock-CN.list
          wget -O ovale-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillAdBlock-Lean.list
          wget -O oval-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillAdBlock-Lite.list
          wget -O ova-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillAdBlock.list
          wget -O ovdle-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillDirect-Lean.list
          wget -O ovd-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillDirect.list
          wget -O ovple-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillProxy-Lean.list
          wget -O ovp-old.list https://raw.githubusercontent.com/dtcokr/v2ray-rules-dat/release/OverkillProxy.list
          
          OVA_OLD=$(wc -l < ova-old.list)
          OVACN_OLD=$(wc -l < ovacn-old.list)
          OVAL_OLD=$(wc -l < oval-old.list)
          OVALE_OLD=$(wc -l < ovale-old.list)
          OVD_OLD=$(wc -l < ovd-old.list)
          OVDLE_OLD=$(wc -l < ovdle-old.list)
          OVP_OLD=$(wc -l < ovp-old.list)
          OVPLE_OLD=$(wc -l < ovple-old.list)

          D_OVA=$(echo "${OVA} - ${OVA_OLD}" | bc)
          D_OVACN=$(echo "${OVACN} - ${OVACN_OLD}" | bc)
          D_OVAL=$(echo "${OVAL} - ${OVAL_OLD}" | bc)
          D_OVALE=$(echo "${OVALE} - ${OVALE_OLD}" | bc)
          D_OVD=$(echo "${OVD} - ${OVD_OLD}" | bc)
          D_OVDLE=$(echo "${OVDLE} - ${OVDLE_OLD}" | bc)
          D_OVP=$(echo "${OVP} - ${OVP_OLD}" | bc)
          D_OVPLE=$(echo "${OVPLE} - ${OVPLE_OLD}" | bc)

          [[ ${D_OVA} -gt 0 ]] && D_OVA=+${D_OVA}
          [[ ${D_OVACN} -gt 0 ]] && D_OVACN=+${D_OVACN}
          [[ ${D_OVAL} -gt 0 ]] && D_OVAL=+${D_OVAL}
          [[ ${D_OVALE} -gt 0 ]] && D_OVALE=+${D_OVALE}
          [[ ${D_OVD} -gt 0 ]] && D_OVD=+${D_OVD}
          [[ ${D_OVDLE} -gt 0 ]] && D_OVDLE=+${D_OVDLE}
          [[ ${D_OVP} -gt 0 ]] && D_OVP=+${D_OVP}
          [[ ${D_OVPLE} -gt 0 ]] && D_OVPLE=+${D_OVPLE}

          echo "D_OVA=${D_OVA}" >> $GITHUB_ENV
          echo "D_OVACN=${D_OVACN}" >> $GITHUB_ENV
          echo "D_OVAL=${D_OVAL}" >> $GITHUB_ENV
          echo "D_OVALE=${D_OVALE}" >> $GITHUB_ENV
          echo "D_OVD=${D_OVD}" >> $GITHUB_ENV
          echo "D_OVDLE=${D_OVDLE}" >> $GITHUB_ENV
          echo "D_OVP=${D_OVP}" >> $GITHUB_ENV
          echo "D_OVPLE=${D_OVPLE}" >> $GITHUB_ENV

      - name: Build geosite.dat file
        run: |
          cd custom || exit 1
          go run ./ --datapath=../community/data
          
      - name: Move and zip files and generate sha256 hash
        run: |
          xz -9 ./reject-list-with-tag.txt
          xz -9 ./CHANGELOG.md
          
          install -Dp ./geoip.dat ./publish/geoip.dat
          install -Dp ./geoip.dat.sha256sum ./publish/geoip.dat.sha256sum
          install -Dp ./custom/publish/geosite.dat ./publish/geosite.dat
          install -p {proxy,direct,reject}-tld-list.txt ./publish/
          install -p {proxy,direct,reject}-list.txt ./publish/
          install -p {apple-cn,google-cn,gfw,greatfire,win-spy,win-update,win-extra}.txt ./publish/
          
          install -p {OverkillAdBlock,OverkillDirect,OverkillProxy,OverkillGoogleCN-DIRECT,OverkillAppleCN-DIRECT}.list ./publish/
          install -p OverkillAppleCN-DIRECT-RuleSet.list ./publish/
          install -p OverkillAdBlock-{CN,Lite}.list ./publish/
          install -p Overkill{AdBlock,Direct,Proxy}-Lean.list ./publish/
          install -p Overkill{AdBlock,Direct,Proxy}-Lean-RuleSet.list ./publish/
          install -p OverkillGuardian.list ./publish/
          
          install -p reject-list-with-tag.txt.xz ./publish/
          
          install -p CHANGELOG.md.xz CHANGELOG.txt ./publish/
          
          install -Dp direct.txt ./hid/direct.txt
          install -p {proxy,reject}.txt ./hid/
          install -p adlist_tagging.py ./hid/
          install -p domain_generalization.py ./hid/
          install -p cn_reject.py ./hid/
          install -p findRedundantDomain.py ./hid/
          install -p find_upper.py ./hid/
          install -p {direct,proxy,reject}-ip.txt ./hid/

          cd ./publish || exit 1
          zip rules.zip {proxy,direct,reject}-list.txt geoip.dat geosite.dat {OverkillAdBlock,OverkillDirect,OverkillProxy,OverkillGoogleCN-DIRECT,OverkillAppleCN-DIRECT}.list OverkillAdBlock-{CN,Lite}.list Overkill{AdBlock,Direct,Proxy}-Lean.list
          sha256sum geosite.dat > geosite.dat.sha256sum
          sha256sum rules.zip > rules.zip.sha256sum
      
      - name: Release and upload assets
        uses: softprops/action-gh-release@59c3b4891632ff9a897f99a91d7bc557467a3a22
        with:
          name: ${{ env.RELEASE_NAME }}
          tag_name: ${{ env.TAG_NAME }}
          draft: false
          prerelease: false
          files: |
            ./publish/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Push to "hidden" branch
        run: |
          cd hid || exit 1
          git init
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b hidden
          git add .
          git commit -m "${{ env.RELEASE_NAME }}"
          git remote add origin "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
          git push -f -u origin hidden
          
      - name: Git push assets to "release" branch
        run: |
          cd publish || exit 1
          git init
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b release
          git add .
          git commit -m "${{ env.RELEASE_NAME }}"
          git remote add origin "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
          git push -f -u origin release
          echo "FDATE=$(date +%Y-%m-%d@%H:%M)" >> $GITHUB_ENV

      - name: Call Telegram
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.CHAT_ID }}
          token: ${{ secrets.BOT_TOKEN }}
          message: |
            #V2RayRulesDatUpdated #GitHub
            
            === Count ===
            `Adblock`: *${{ env.OVA }}* (${{ env.D_OVA }})
            `Direct`: *${{ env.OVD }}* (${{ env.D_OVD }})
            `Proxy`: *${{ env.OVP }}* (${{ env.D_OVP }})

            `Adblock-CN`: *${{ env.OVACN }}* (${{ env.D_OVACN }})
            `Adblock-Lite`: *${{ env.OVAL }}* (${{ env.D_OVAL }})

            `Adblock-Lean`: *${{ env.OVALE }}* (${{ env.D_OVALE }})
            `Direct-Lean`: *${{ env.OVDLE }}* (${{ env.D_OVDLE }})
            `Proxy-Lean`: *${{ env.OVPLE }}* (${{ env.D_OVPLE }})
            
            === Date ===
            ${{ env.FDATE }} UTC
          format: markdown
          disable_notification: true
